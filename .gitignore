# Build artifact — regenerated by build.sh
YouTube Music.app/

# Distributable produced by package.sh
dist/
*.zip

# Node deps — bundled into the .app at build time, never source
node_modules/

# Logs
*.log

# Tooling / index databases (ruflo, ruvector, etc.)
*.db

# Icon build scratch
.iconset/

# macOS cruft
.DS_Store
